# 无登录采样 + 话题热点推断

## 一、前置方案思路及其废弃原因

| # | 方案 | 思路 | 废弃原因 |
|---|---|---|---|
| 1 | **rebang.today / 三方热榜聚合站** | 直接抓第三方平台的"小红书热搜"页面 | 数据已经长期无更新，不可用 |
| 2 | **(登录态)平台推荐+验证** | 登录账号拿"猜你想搜" + 判断是否为热点 | 受帐号个性化影响过重，拿到的词条大概率不是全站热点，就算少量情况下确是热点，拿到后到判断链条过长，并且一段时间内平台推送的“猜你想搜”不会变化；含 `ads` 商业位 |
| 3 | **Guest 模式探测热榜接口** | 不登录直接探 `/hot_list`、`/board/list`、`trending/query` 等 | 全部 406 / 失败；PC 站游客态搜索框 placeholder 直接是"登录探索更多内容"，搜索功能被禁用 |
| 4 | **jieba / N-gram 对 desc + 评论分词** | 用户初版尝试 | 用户明确否决："切碎的看不出来意义的词语完全不符合需求"。词条无法直接作为话题展示 |

**核心结论**：登录态 / 第三方源 / 暴力分词都不可行；唯一干净的一手信号是
小红书内容里**作者主动打的 `#话题#` 标签**。

## 二、当前方案思路

> **无登录推荐流采样 → 完整 `#话题#` 抽取 → 多维归一化打分 → 热点判定**

### 数据采集（`crawler/xhs_hotpost/noauth_fetcher.py`）

- Playwright 持久化 visitor profile（`browser_profile/xhs_noauth/`），让风控
  把这台设备老化为"普通游客"，多次跑后评论接口可从 461 → 200。
- 滚动 explore 触发 `/api/sns/web/v1/homefeed` 拿推荐流，记录笔记在推荐位的
  `rank`，同时回填 DOM 上的 `xsec_token`。
- 逐条 goto 详情，并行拦截 `note/detail` + `comment/page`，从三条路径
  抽取 `tag_list`：
  1. detail API JSON（`note.tag_list[]` → `{type:"topic",name}`）
  2. 详情页 `__INITIAL_STATE__` 注入对象（`note.tagList`）
  3. HTML 兜底正则（`"tag_list":[…]`、`"desc":"…"`）

### 话题抽取（`xhs_content_trend/scorer.py`）

- 完全不引入分词器。两条规则：
  - 结构化 `tag_list` 直接采纳
  - 文本里 `#XXX[话题]#` 字面正则
- 校验：长度 2–30，必须含 CJK 或字母数字，避免符号噪声。

### 多维打分

```text
total = 0.30 * coverage     # 命中笔记数
      + 0.30 * engagement   # log1p(命中笔记总互动)
      + 0.20 * exposure     # 1/log2(rank+1.5) 加权
      + 0.15 * comment      # 评论提及次数
      + 0.05 * density      # 同笔记内重复
```

各维度 max–min 归一到 0–100，互动公式：
`liked + 2·comments + 1.5·collected + 3·shares`。

### 热点判定

```text
total_score >= 40   AND   命中笔记数 >= 2   AND   命中笔记互动总和 >= 1000
```

## 三、相比前几版的提升

- **可解释**：输出的是完整可读话题（"明日方舟七周年" / "RED明星热力风向标"），
  而不是 "明日 / 方舟 / 周年" 一类碎片词。
- **干净的一手信号**：直接来自小红书作者打的官方话题标签，不掺第三方倒卖、
  不掺账号个性化、不掺商业广告位。
- **完全无登录**：不需要小号、不需要 cookie 维护，跑就完事。
- **多维而非单一互动**：覆盖广度 + 互动 + 推荐位 + 评论共鸣，单条爆款笔记
  无法独自把一个话题抬成热点。
- **判定门槛硬阈值**：避免"只有 1 篇笔记的孤岛话题"被误判热点。

## 四、局限

- **样本稀疏**：无登录态下单次只有 ~20%–40% 笔记能成功拿到完整 `tag_list`
  （detail API 不稳定，desc 经常空）。需要多轮累积才能形成稳定排序。
- **覆盖偏倚**：homefeed 本身就是平台推荐策略产物，所谓"热点"实际是
  *游客视角下平台愿意推的话题*，并非全站真实搜索热度。
- **评论提及维度近乎失效**：小红书评论 UI 把 hashtag 渲染为可点击链接而非
  纯文本，文本中很难直接抓到 `#话题#`，`comment_score` 多为 0。
- **无跨时间窗合并**：当前每次跑都是孤立批次。要真正识别"新晋热点"需要
  叠加历史快照做环比，目前尚未实现。
- **风控漂移**：profile 老化失效后评论接口会回到 461，需要重新养号或
  调整指纹策略；不是一劳永逸。
- **话题层级噪声**：作者打的标签里混有"穿搭 / 居家 / 搞笑"这类极泛标签，
  排名靠前的不一定是真热点，可能只是高频品类。后续可加 IDF / 长期均值
  下压处理。
